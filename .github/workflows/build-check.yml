name: build-check

# Fails if the committed draw.io library or svg/ files are out of sync with
# icons-data.js. Regenerate locally with `node build.js` and commit the result.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Verify generated files are up to date
        run: node build.js --check
